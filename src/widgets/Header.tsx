// 페이지를 구성하는 독립적인 UI 블록
// 헤더 (로고, 프로필 메뉴 등)
// Vanilla CSS 작성 후 디렉터리화 or Tailwind 추가하기 

const Header = () => {
    return (
        <header>
            <h1>EX</h1>
            <p>login</p>
        </header>
    )
}

export default Header();